# site-d.example · produccion VIEJA de mentira (fake-production.pl)
# Los arboles salen de _generar.pl (este y fixtures-repos/site-d-web).
#
# El receptor del formulario: el proxy no ejecuta PHP. Un receptor bien puesto
# contesta 405 a un GET (el metodo real es POST), que NO es un 404.
status /enviar.php 405
# Cabeceras de cache como las de un host estatico. Sin ?v= en ninguna URL, asi
# que REN-09 (sellado + no-store) no tiene nada que acusar.
header /img/* Cache-Control: public, max-age=31536000
header /styles.css Cache-Control: public, max-age=3600
header /script.js Cache-Control: public, max-age=3600
